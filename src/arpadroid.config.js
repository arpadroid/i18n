/** @type {import('@arpadroid/module').BuildConfigType} */
const config = {
    buildTypes: true,
    buildType: 'uiComponent',
    buildManifest: true,
    buildStyles: false,
    storybook_port: 6004
};

export default config;
